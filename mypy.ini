[mypy]
python_version = 3.12
ignore_missing_imports = True
explicit_package_bases = True
# Gradual typing: agent pipeline uses Optional locals filled by control flow
# that mypy cannot prove (union-attr / arg-type). Keep structural checks on.
disable_error_code = union-attr, arg-type
