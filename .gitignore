# Python
.venv/
backend/.venv/
__pycache__/
*.pyc
.coverage
.coverage.*
htmlcov/
backend/.coverage
.mypy_cache/
.pytest_cache/
.ruff_cache/
backend/.pytest_cache/
backend/.mypy_cache/

# Local vector store (regenerated per machine)
backend/.chroma/

# Node
node_modules/
frontend/node_modules/
frontend/dist/

# Environment files
backend/.env
frontend/.env

# Kubernetes local secrets (never commit real keys)
k8s/secret.yaml

# Terraform local state / providers (local minikube demo)
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.*
terraform/.terraform.lock.hcl

# IDE / OS
.vscode/
.DS_Store
