# Local development only — single Redis for the RQ review queue.
# Not a production/HA setup.
services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - personacr_redis:/data
    command: ["redis-server", "--appendonly", "yes"]

volumes:
  personacr_redis:
