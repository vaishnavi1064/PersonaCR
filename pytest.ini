[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
markers =
    groq: tests that call Groq / need network LLM (exclude from fast CI with -m "not groq")
    slow: tests that load embedding models or hit local vector DB
filterwarnings =
    ignore::DeprecationWarning
